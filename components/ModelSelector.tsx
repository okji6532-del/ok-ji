import React from 'react';

// This component is deprecated as SDXL support has been removed.
// It is kept as a placeholder to prevent import errors during the transition if referenced elsewhere.
export const ModelSelector: React.FC<any> = () => {
  return null;
};